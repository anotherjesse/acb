OPENAI_API_KEY=replace_with_openai_api_key

# Optional override for orchestrator YAML config path.
# Default: ./config/matrix-orchestrator.yaml
# MATRIX_ORCHESTRATOR_CONFIG=/Users/jesse/oss/rc/config/matrix-orchestrator.yaml

# Bridge/runtime session persistence path (inside task spark)
# BRIDGE_SESSION_FILE=data/bridge-sessions.json

# Optional Matrix sync tuning for bridge/orchestrator loops
# MATRIX_SYNC_TIMEOUT_MS=30000

# Optional CLI log level: debug | info | warn | error
# LOG_LEVEL=info

# Optional Codex settings
# CODEX_MODEL=gpt-5-codex
# CODEX_WORKING_DIRECTORY=/work
# CODEX_SKIP_GIT_REPO_CHECK=true
# CODEX_NETWORK_ACCESS=true
# CODEX_ADDITIONAL_DIRECTORIES=/spark/all

# YOLO mode convenience toggle:
# Equivalent to sandbox=danger-full-access + approval=never unless explicit values are set.
# CODEX_YOLO=true

# Explicit overrides (take precedence over CODEX_YOLO behavior if set)
# CODEX_SANDBOX_MODE=workspace-write
# CODEX_APPROVAL_POLICY=on-request
